version: "3"
services:
    webapplication3:
        build: 
          context: .
          dockerfile: Dockerfile
        ports:
            - "8080:80"
        depends_on:
            - redis
    redis:
        image: redis
        ports:
            - "6379:6379"